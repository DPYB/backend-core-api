name: CI

on:
  pull_request:
    branches: [develop, main]
  push:
    branches: [develop, main]

jobs:
  ci:
    uses: DPYB/.github/.github/workflows/reusable-python-ci.yml@main
    with:
      python-version: "3.12"
